---
description: Convert an academic paper (arXiv URL/ID, local PDF, or pasted text) into a Substack-style article saved to output/<slug>.md.
agent: paper-to-substack
---

Convert the following input into a Substack-style article and save it to `output/<slug>.md`:

$ARGUMENTS

Follow the full workflow in your system prompt. Report the output path, chosen title, word count, and a 2-sentence summary when done.
